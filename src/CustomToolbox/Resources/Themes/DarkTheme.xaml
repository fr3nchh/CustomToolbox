<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:CustomToolbox.Converters">

    <!-- ============================================ -->
    <!-- PALETTE : ReviOS / Atlas OS Style           -->
    <!-- ============================================ -->

    <!-- Accent -->
    <Color x:Key="PrimaryColor">#6C63FF</Color>
    <Color x:Key="PrimaryLightColor">#7B73FF</Color>
    <Color x:Key="PrimaryDarkColor">#5A52D9</Color>

    <!-- Backgrounds : tons de gris très sombres -->
    <Color x:Key="BackgroundColor">#1A1A1A</Color>
    <Color x:Key="SurfaceColor">#1E1E1E</Color>
    <Color x:Key="CardColor">#252526</Color>
    <Color x:Key="CardHoverColor">#2A2A2C</Color>
    <Color x:Key="SidebarColor">#1E1E1E</Color>
    <Color x:Key="HoverColor">#2C2C2C</Color>

    <!-- Texte -->
    <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#999999</Color>
    <Color x:Key="TextDisabledColor">#555555</Color>

    <!-- Status -->
    <Color x:Key="SuccessColor">#4CAF50</Color>
    <Color x:Key="WarningColor">#FF9800</Color>
    <Color x:Key="ErrorColor">#F44336</Color>
    <Color x:Key="BorderColor">#333333</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="CardHoverBrush" Color="{StaticResource CardHoverColor}"/>
    <SolidColorBrush x:Key="SidebarBrush" Color="{StaticResource SidebarColor}"/>
    <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextDisabledBrush" Color="{StaticResource TextDisabledColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

    <!-- Converters -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
    <converters:CategoryToIconConverter x:Key="CategoryToIconConverter"/>
    <converters:AppIconConverter x:Key="AppIconConverter"/>
    <converters:AppLetterIconConverter x:Key="AppLetterIconConverter"/>
    <converters:AppFirstLetterConverter x:Key="AppFirstLetterConverter"/>

</ResourceDictionary>
